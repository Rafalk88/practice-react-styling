import * as React from "react";
import { StyledBreadcrumb } from "./Breadcrumb.styled";

const Breadcrumb = (props) => {
  return <StyledBreadcrumb>{"Home"}</StyledBreadcrumb>;
};

export default Object.assign(Breadcrumb, {});
